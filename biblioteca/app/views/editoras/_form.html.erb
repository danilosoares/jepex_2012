<%= form_for(@editora, :html => { :class => "form-horizontal"}) do |f| %>
  <% if @editora.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@editora.errors.count, "error") %> prohibited this editora from being saved:</h2>

      <ul>
      <% @editora.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :nome, :class => "control-label" %>
    <div class="controls">
      <%= f.text_field :nome, :class => "span8" %>
    </div>
  </div>
  
    <div class="form-actions">
      <input type="submit" class="btn btn-primary" value="Salvar"/>
      <%= link_to 'Voltar', editoras_path, :class => "btn" %>
  </div>
<% end %>
